import React, { memo } from 'react'

const Loading = memo(() => {
  return (
    <div className='w-100 vh-100 bg-white d-flex align-items-center justify-content-center'>
      Loading
    </div>
  )
})

export default Loading